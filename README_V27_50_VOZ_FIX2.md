# ZAR v27.50 — Voz PRO FIX 2

La ruta de voz usa primero el endpoint directo `generateContent` de Gemini con audio WAV inline y, si no devuelve texto, prueba el endpoint OpenAI-compatible documentado por Google. La interfaz y el resto del proyecto se conservan.
