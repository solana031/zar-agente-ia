# ZAR v29.0.2 — UI File Fix

- Restores the missing `formatBytes()` helper used by the file explorer.
- Keeps the v29 model router and all v29.0.1 functionality.
- Removes a hard-coded model-name claim from the visible Control ZAR route heading so the UI does not contradict the configured model.
- No API keys or secrets included.
