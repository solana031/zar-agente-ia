# ZAR v30.10.35 — Mobile Studio shell fix

- Mobile Studio now completely replaces the chat/home surface.
- The ZAR home logo, floating quick actions, active-learning pill and chat composer are hidden while `editor-mode` is active.
- Studio keeps its own vertical scroll area on mobile.
- Studio header and mode tabs remain accessible without interfering with the chat composer.
- Existing Studio functionality is preserved.
